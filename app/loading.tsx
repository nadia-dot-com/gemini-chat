import { LoadingSpinner } from "@/components/ui/LoadingSpinner";

export default function Loading() {
  return (
    <div className="flex align-center justify-center h-[100dvh]">
      <LoadingSpinner/>
    </div>
  );
}
